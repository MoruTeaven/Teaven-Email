{
  "name": "teaven-email",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy src/index.ts",
    "db:migrate": "wrangler d1 migrations apply teaven-email-db --remote"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250530.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.5.0"
  }
}
