{
  "name": "teaven-email",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute teaven-email-db --remote --file=./migrations/001_init.sql"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250530.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.5.0"
  }
}
